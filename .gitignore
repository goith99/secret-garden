.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
.surfpool

# Arcium build artifacts (regenerated by `arcium build`)
build
artifacts/
encrypted-ixs/arcis_temp_target
encrypted-ixs/src/main.rs

# Environment files (never commit real secrets)
.env
.env.*
!.env.example

# Solana / wallet keypairs (never commit private keys)
*-keypair.json
*.keypair.json
id.json
keypairs/
wallets/

# Local AI tool config (never commit)
.claude/
