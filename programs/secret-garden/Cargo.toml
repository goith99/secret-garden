[package]
name = "secret-garden"
version = "0.1.0"
description = "Secret Garden Protocol — Stage 1 on-chain foundation"
edition = "2021"
license = "ISC"

[lib]
crate-type = ["cdylib", "lib"]
name = "secret_garden"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "arcium-anchor/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []


[dependencies]
anchor-lang = { version = "1.0.2", features = ["init-if-needed"] }
# Required by `#[account(zero_copy)]` (Tier1State): the SAFE form derives bytemuck's
# Pod/Zeroable, which additionally REFUSES TO COMPILE if the struct has any implicit
# padding — that check is exactly what keeps the zero-copy layout honest.
bytemuck = { version = "1.17", features = ["derive", "min_const_generics"] }
# SHA-256 over the offspring ciphertext for the genome commitment (BPF-compatible,
# already in the lockfile transitively). anchor_lang::solana_program strips the hasher
# modules, so depend on the hasher crate directly.
solana-sha256-hasher = "3.1.0"
arcium-client = { default-features = false, version = "=0.14.1" }
arcium-macros = "=0.14.1"
arcium-anchor = "=0.14.1"
# pin so that we do not have 1.13.*
unicode-segmentation = { version = "=1.12.0" }
# pin so that we do not have edition = 2024
hashbrown = { version = "=0.16.1" }
indexmap = { version = "=2.13.1" }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
